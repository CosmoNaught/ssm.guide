# No build/runtime dependencies yet: the book is prose + math only.
# Python packages (e.g. numpy, matplotlib, jupyter, torch, jax) will be added
# here when executable code examples are introduced in later chapters.
