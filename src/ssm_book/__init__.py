"""Reference implementations for the State Space Models book.

Organised by backend: ``ssm_book.numpy`` is the reference; ``ssm_book.torch``
and ``ssm_book.jax`` are ports checked against it in ``tests/``.
"""

__version__ = "0.1.0"
